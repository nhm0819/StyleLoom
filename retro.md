# 회고

## 출발한 가정

1. **스타일은 "측정할 숫자"와 "이름 붙일 언어"로 쪼갤 수 있다.** 페이싱과 색은 픽셀에서 재고,
   그레이드·카메라 어휘·룩은 LLM이 *명명*만 한다. LLM이 "빠른 컷, 강한 색"이라고 답하는 건
   재현도 검증도 안 되기 때문이다. → **유지됐다.** QC가 같은 측정기로 결과물을 재측정할 수 있는
   이유가 이 분리다.
2. **비결정성은 특정 지점에 설계해 넣어야 한다.** temperature에 맡기면 "다를 수 있다"가 우연이 된다.
   → **유지됐고, 나중에 결정적이었다.** temperature를 API에서 빼자 아키타입 추첨과 softmax 선택만
   남았는데 훅은 여전히 재실행마다 달라진다.
3. **컷 사이 정체성은 앵커 이미지를 매 요청에 실어야만 유지된다.** → 유지됐다. 다만 대가가 있다:
   요청당 이미지 1장.
4. **QC가 결과물을 재측정해야 "시스템"이라는 주장이 검증 가능해진다.** → **절반만 유지됐다.**
   QC는 아직 report-only다. 실패해도 런은 DONE이고 게이트가 없다.

## 어디서 막혔는가

**API 세대 불일치가 조용히 성공한다.** 코드는 레거시 `/v1/videos/*` 형태로 쓰여 있는데 타깃은
Kling 3.0이었다. 3.0에서 `model_name`·`mode`·`negative_prompt`는 **모르는 키라서 무시**되고,
요청은 `succeeded`로 돌아오며 기본값 5초가 생성된다. 폴링도 `data.task_id`→`data.id`,
`succeed`→`succeeded`, 단일 객체→배열로 전부 달랐다. 문서를 코드와 한 줄씩 맞춰보기 전까지
드러나지 않았다.

**두 엔드포인트의 글자수 예산이 5배 차이인데 그걸 모른 채 프롬프트를 짰다.** 샷당 512자(하드 리밋),
이미지 2500자. 측정해 보니 고정 스타일 토큰이 512자 중 **415자**를 먹고 실제 샷 묘사에 40자만
남았으며, 부족분은 `fit()`이 **QC가 채점하는 컬러 그레이드를 조용히 버려서** 메꾸고 있었다.
그리고 레퍼런스 묘사 7개는 이미지 프롬프트에 **0개** 도달했다 — 2500자 예산의 8%만 쓰면서.

**1초 미만 페이싱은 렌더할 수 없다.** 레퍼런스는 0.76초마다 컷하고 엔드포인트의 최소 컷은 1초다.
엔드포인트 격자에 맞춰 계획하는 쪽을 택했고, 그래서 페이싱은 **구조적으로 31% 어긋난다.**
QC 허용치를 그 강제 격차만큼만 넓히고, 넓힘이 적용되면 "통과가 재현을 뜻하지 않는다"고 경고한다.

**공통 패턴: 조용히 실패하는 것들.** ffmpeg는 프레임 밖에 자막을 그려도 에러를 내지 않는다
(폰트 크기를 높이에서 뽑아 9:16에서 양쪽이 잘렸다). `multi_shot`의 기본값은 `true`다.
그리고 **오프라인 프로바이더가 선언하지 않은 제약은 유료 실행 전까지 아무도 검증하지 않는다** —
프롬프트 상한, 요청당 샷 수(14컷이 1요청으로 통과), 정수초 격자에서 같은 실수를 세 번 반복했다.

## 어떤 모델로 실험했고 무엇을 얻었는가

**정직하게 적어야 하는 부분이다.** 이 저장소에서 end-to-end로 검증한 것은 **오프라인 경로**다
(324개 테스트, 네트워크·키 없음). 유료 엔드포인트로 **출력 품질을 나란히 비교하지 않았다.**

**실측한 것**
- 자막 기하: 5가지 프레임 형태에서 픽셀 바운딩 박스 측정 → 12자 한 줄이 720px 프레임에서
  720px로 잘리던 것을 594px로. 위치·줄간격도 픽셀로 검증.
- 프롬프트 예산: 이미지 프롬프트 212→561자, 레퍼런스 묘사 도달 0/7→5개 차원 전부.
- 윈도우 패킹 산술: 14컷 → 6/6/2 세 요청, duration 6/6/3초, 합계가 `settings.duration`과 일치.
- 길이 충실도: 요청 = 과금 = 산출. 이전엔 10.7초 영상에 15초 과금(40% 초과).
- 훅 비결정성: 반복 실행에서 아키타입·텍스트 모두 1종 초과, recency penalty 실효 확인.

**문서 기반으로 선택했고 품질은 비교하지 못한 것**
- **Kling 3.0 i2v**를 기본으로: `contents`의 타입 있는 first_frame과 요청당 샷 리스트가
  레거시·omni에는 없다. 레거시 자격증명은 3.0 이후 모델을 못 쓴다.
- **Kling Image**: 참조 이미지를 받아 같은 인물을 재생성하는 경로라서. 이미지 품질 비교는 안 했다.
- **Claude Sonnet**: 한국어 카피와 vision을 한 모델로. JSON 안정성은 스키마 지시로 확보.

**추론이 아니라 측정으로 답해야 하는 질문** — 유료 키로 15초 창 하나면 나온다:
Kling이 세미콜론 샷 리스트의 컷 타이밍을 실제로 지키는가. 오프라인 drift는 **구조적으로 0**이다
(오프라인 렌더러가 곧 ffmpeg이므로). 이게 윈도우 렌더링을 신뢰할지 결정하는 단 하나의 실험이다.
색상 QC 항목도 실제 렌더에서만 의미가 있다 — mock은 그레이드를 맞추려는 시도를 아예 하지 않으므로
**정당하게** 실패한다. mock이 자기 QC를 통과하게 만들면 QC가 무의미해진다.

## 시간이 더 있었다면

1. **1초 미만 페이싱 복구.** 윈도우 클립을 계획된 컷 경계에서 쪼개 각 조각을 요청 길이로 트림한 뒤
   재결합. 윈도우당 ffmpeg 1패스, **생성 비용 증가 없음.** 페이싱과 길이를 동시에 되찾는다.
2. **QC를 게이트로.** 지금은 report-only다. 아래 설계를 구현한다.
3. **샷 사이즈 분포 측정.** 지금은 스키마 기본값으로 폴백한다. 피사체 검출이 필요하다.
4. **전환 어휘(match cut·whip pan)를 스키마에.** 지금은 `subject`에 뭉쳐 들어간다.
5. **`look.detail` 마이그레이션.** 기존 추출 스타일은 평면 `keywords`로 폴백하는데, 그러면
   분리가 해결하려던 문제(이미지 프롬프트에 "no talking head"가 들어가는)가 재현된다.

## (가산점) 레퍼런스 기준 충족을 시스템이 스스로 판정하게 하려면

**1단계 — 숫자 게이트 (이미 있는 것을 게이트로 바꾸기).** QC는 이미 페이싱·총길이·색 3축·훅 윈도우
컷 수·컷 드리프트를 스타일 대비로 잰다. 여기에 항목별 **심각도**를 부여한다: 색은 재시도로 고칠 수
있고, 컷 드리프트는 요청 구조를 바꿔야 하고, 길이는 계획을 다시 세워야 한다.

**2단계 — 실패 항목이 재시도 방식을 지정한다.** 재추첨(re-roll)은 안 된다. 무엇이 틀렸는지 알면서
같은 주사위를 다시 굴리는 것이기 때문이다. 대신 실패 항목 → 입력 변경이 1:1로 대응해야 한다.
색 실패 → 이미지 프롬프트의 `palette`·`grade` 강화. 드리프트 실패 → 윈도우당 샷 수 감소.
길이 실패 → 격자 재계획. 각 재시도는 무엇을 왜 바꿨는지 런 폴더에 남긴다.

**3단계 — VLM 심판, 단 숙제를 자기가 채점하지 않게.** 숫자로 볼 수 없는 것(같은 인물인가, 제품이
보이는가, 레퍼런스처럼 보이는가)은 결과물 프레임과 **레퍼런스 프레임을 나란히** 놓고 VLM에 묻는다.
결정적 제약: 심판에게 **스타일 추출에 쓴 그 프레임을 주면 안 된다.** 자기가 만든 라벨을 근거로
자기 출력을 채점하는 순환이 된다. 레퍼런스에서 별도로 홀드아웃 프레임을 떼어 둬야 한다.

**4단계 — 진짜 주장은 자기일관성이다.** "같은 시스템이 다른 입력을 같은 스타일로 만든다"는 명제는
한 편으로는 증명되지 않는다. 같은 입력을 두 번 돌린 결과가, 다른 입력으로 만든 결과보다 **측정
축에서 서로 더 가까워야** 한다. 이건 사람의 판단 없이 계산 가능하고, 하네스가 스스로 돌릴 수 있는
유일한 "스타일을 지켰는가" 검사다. 지금 없는 것이고, 있다면 위의 게이트 임계값을 스타일마다
자동으로 정하는 근거도 된다.
