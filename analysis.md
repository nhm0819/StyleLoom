# 사용 도구 · 무엇을 왜 선택했는가

각 항목 1~2줄. 근거를 더 길게 쓴 판단은 [docs/TOOL_RATIONALE.md](docs/TOOL_RATIONALE.md)에 있고,
엔드포인트별 스펙과 제약은 코드가 아니라 데이터로 [configs/kling_models.yaml](configs/kling_models.yaml)에 있다.

## AI 모델

| 도구 | 어디에 쓰는가 | 왜 이것인가 |
|---|---|---|
| **Claude** (`claude-sonnet-5`, Anthropic Messages API) | 입력 정규화(ingest), 본문 비트(outline), 훅 후보 생성, 레퍼런스 스타일 **언어 라벨링** | 한국어 숏폼 카피와 vision을 한 모델로 처리할 수 있어 스테이지마다 모델을 바꾸지 않아도 된다. 숫자는 만들지 못하게 지시하고 픽셀 측정값만 쓴다. |
| **Kling Image** (`/v1/images/generations`, `kling-v3`) | 런의 아이덴티티 앵커 1장 + 렌더 요청당 리드 프레임 1장 | 참조 이미지를 받아 같은 인물을 재생성할 수 있어야 컷 사이 정체성이 유지된다. 프롬프트 상한이 2500자라 레퍼런스 묘사를 여기에 몰아넣는다. |
| **Kling 3.0 image-to-video** (`/image-to-video/kling-3.0`) | 프레임 → 영상. 프로덕션 경로 | `contents`에 **타입이 있는** first_frame을 실어 "시작 프레임"과 "느슨한 스타일 참조"가 구분된다. 요청 1회에 최대 6컷을 담아 길이 하한을 컷마다 물지 않는다. |
| **Kling 3.0 text-to-video** (`/text-to-video/kling-3.0`) | `USE_FIRST_FRAME=false`일 때만 쓰는 폴백 | 프레임 없이도 돌아가게 두되 기본 경로에서 뺐다. 텍스트만으로 생성한 컷은 앞뒤 컷과 정체성을 공유하지 않는다. |

**모델 선택에서 실제로 갈렸던 지점**: 레거시 `/v1/videos/*`와 `kling-v3-omni`도 스펙 파일에 남겨 두었지만 기본값은 3.0이다.
3.0만이 요청당 샷 리스트·타입 있는 first_frame·`/tasks` 폴링을 갖고 있고, 레거시 자격증명은 3.0 이후 모델을 지원하지 않는다.

## 외부 API · 인증

| 도구 | 왜 |
|---|---|
| **Kling API Key** (단일 키, `Authorization: Bearer <key>`) | 예전 access/secret 쌍은 클라이언트가 30분짜리 HS256 JWT를 직접 서명해야 했고 3.0 이후 모델을 못 쓴다. 단일 키는 서명도 만료도 없다. |
| **폴링 (`GET /tasks`)** — 콜백 미사용 | 콜백은 공개 HTTP 리스너가 필요해 CLI를 서비스로 만든다. 폴링은 단일 운영자 하네스에 맞는 비용이다. |

## 오픈소스

| 도구 | 왜 |
|---|---|
| **ffmpeg** | 자막 번인(`drawtext`)과 결합(`concat`). 자막을 영상 모델에 맡기면 글자가 매 영상 달라져 "한 채널"로 읽히지 않는다. 폰트·위치·자간이 바이트 단위로 동일해야 한다. |
| **OpenCV** (`opencv-python-headless`) | 컷 검출·색 통계·이미지 크기. 페이싱과 그레이드는 숫자이므로 픽셀에서 측정해야 재현·검증이 가능하다. headless는 GUI 의존성을 뺀다. |
| **NumPy** | OpenCV 프레임 연산의 기반. 히스토그램 거리와 프레임 샘플링에 쓴다. |
| **Pydantic v2 + pydantic-settings** | `style.json`·런 산출물의 스키마와 환경변수 설정. 손으로 고친 스타일 파일이 조용히 깨지는 대신 검증에서 걸린다. |
| **Typer** | CLI. 타입 힌트에서 인자·헬프가 나오고, 서브커맨드 그룹으로 `run` / `run resume`를 묶을 수 있다. |
| **httpx** | HTTP 클라이언트. 타임아웃과 `raise_for_status`가 명시적이다. |
| **PyYAML** | 엔드포인트 스펙·훅 아키타입·캐스팅 풀. 코드 배포 없이 편집 가능한 데이터로 두기 위해. |
| **pytest** | 324개 테스트, 네트워크·키 없이 전부 오프라인. |
| **ruff** | 린트·임포트 정렬. |
| **Docker** | ffmpeg와 CJK 폰트가 포함된 재현 가능한 실행 환경. 자막은 폰트가 없으면 조용히 안 그려진다. |

**의도적으로 쓰지 않은 것**

- **PyJWT** — 단일 API Key로 바뀌어 서명이 사라졌다. (그 전에는 의존성 추가 없이 HS256을 직접 구현했다.)
- **LLM 플래너 / 에이전트 프레임워크** — 플랜은 정적 DAG다. 모델이 스테이지 순서를 바꾸면 출력 차이가 입력 탓인지 알 수 없어져 재현성 주장이 사라진다.
- **DB** — `data/runs/<id>/`의 JSON 파일이 곧 산출물이다. DB는 중간 상태를 감추고 다시 export 단계를 요구한다.
- **Pillow** — OpenCV가 이미 있어 이미지 크기 확인에 두 번째 이미징 의존성을 추가할 이유가 없었다.

## Python

`>=3.11`. `tomllib`와 개선된 타입 문법을 쓰고, 컨테이너 이미지는 3.12를 쓴다.
